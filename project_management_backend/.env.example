# PostgreSQL connection string.
# This can be derived from the database container's db_connection.txt by stripping the leading 'psql '.
# Example: postgresql://appuser:dbuser123@localhost:5000/myapp
DATABASE_URL=

# JWT configuration (set a strong random secret in real deployments).
JWT_SECRET_KEY=
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# CORS allowed origins for frontend dev/prod (comma-separated)
CORS_ALLOW_ORIGINS=http://localhost:3000
